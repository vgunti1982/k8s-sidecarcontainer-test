apiVersion: v1
kind: Service
metadata:
  name: {{ include "nginx-with-otel-sidecar.fullname" . }}
  labels:
    {{- include "nginx-with-otel-sidecar.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nginx-with-otel-sidecar.selectorLabels" . | nindent 4 }}